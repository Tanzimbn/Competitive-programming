Given a certain lattice polygon with non-zero area.
We denote its area by S, the number of points with integer coordinates lying strictly inside the polygon by I and
the number of points lying on polygon sides by B.
S = I + (B/2) − 1
B can be calculated using GCD(| x1−x2 |, | y1−y2 |)+1